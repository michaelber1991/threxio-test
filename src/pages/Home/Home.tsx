"use client";
import React from "react";
import styled from "@emotion/styled";

export type HomeProps = {
  // types...
};

const Home: React.FC<HomeProps> = () => {
  return <HomeStl>Home works!</HomeStl>;
};

export const HomeStl = styled.div``;

export default Home;
